---------------- HTPP on node n2 -------------------
// Startup commands:
startup index: 80

mkdir -p /run/lock
mkdir -p /var/log/apache2
apache2ctl start


// Shutdown commands:
apache2ctl stop

------------------------------------------------------


----------------- FTP on node 2 -------------------

// Startup commands:
chmod a-w /var/run/vsftpd/empty
chmod a-w /var/ftp
vsftpd ./vsftpd.conf

